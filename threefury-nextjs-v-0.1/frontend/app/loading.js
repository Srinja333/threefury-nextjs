import React from 'react';

const Loader = () => {
    return (
        <section className="loading_section">
            <div className="loading">
            </div>
        </section>
    );
    };

export default Loader;